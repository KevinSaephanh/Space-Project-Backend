spring:
  application:
    name: gateway-service
zuul:
  routes:
    auth-service:
      # Map path to auth service
      path: /auth/**
      service-id: AUTH-SERVICE
      
      # Enable pass "/auth/" in the path to auth service
      strip-prefix: false
      
      # Exclude authorization from sensitive headers
      sensitive-headers: Cookie, Set-Cookie